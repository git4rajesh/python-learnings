1. sys.path.extend(['../../config'])

2. os.path.normpath()
3. os.path.dirname(__file__)

4. raise RuntimeError('Refcount is less than zero, check for duplicate disconnects')


5. def read(self, query, params=()):
     The last arguement to read() method is params method or params tupule.

6. SQL script (ORDER BY) 